# Frontend Environment Configuration - Local Override
# Create a .env.local file to override settings locally (not committed to git)
# This file takes precedence over .env.development

# API Configuration
# Example: Override the development backend URL for local testing
# VITE_API_URL=http://localhost:3000
